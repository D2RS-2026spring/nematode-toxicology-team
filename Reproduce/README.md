## 🔧 figure1复现

### 问题 1：Windows 系统环境搭建报错
*   **现象**：执行 bash 脚本时报语法错误，导致 Python 依赖包安装失败。
*   **解决方案**：
    *   使用 `conda create -n nematode-hts python=3.9` 创建独立环境。
    *   通过 `pip install -r requirements.txt` 安装依赖，规避系统冲突。

---

### 问题 2：代码运行提示“变量未定义”
*   **现象**：运行可视化脚本时提示 `NameError: name 'toxicant_conc' is not defined`。
*   **解决方案**：
    *   批量替换错误变量名为原始数据列名 `conc`。
    *   在脚本中添加异常捕获机制，提升代码健壮性。

---

### 复现结论
1.  重金属暴露组存活率统计结果与论文完全一致，核心结论可稳定复现。
2.  有机污染物低剂量组因未固定随机种子，结果存在微小波动，但不影响整体趋势。


## 🔧figure2复现

### 问题 1：Windows 系统环境搭建报错
*   **现象**：执行 bash 脚本时报语法错误，导致 Python 依赖包安装失败。
*   **解决方案**：
    *   使用 `conda create -n nematode-hts python=3.9` 创建独立环境。
    *   通过 `pip install -r requirements.txt` 安装依赖，规避系统冲突。

---

### 问题 2：EC₁₀ 敏感性分布图坐标轴格式不一致
*   **现象**：复现图中 EC₁₀ 数值的分布与原图视觉差异较大，难以直接对比。
*   **解决方案**：
    *   参考原 `01_data_processing.R` 脚本，对 EC₁₀ 数据进行对数转换。
    *   在绘图代码中统一设置 `scale_x_log10()`，匹配原图的数值范围。

---

### 复现结论
1.  跨物种 HTS 整合数据分析结果与论文完全匹配，仅图表配色存在无实质影响的细微差异。
2.  数据预处理流程清晰，关键参数标注明确，可快速复现核心统计结果。


## 🔧 figure3复现

### 问题 1：代码运行提示“变量未定义”
*   **现象**：运行可视化脚本时提示 `NameError: name 'toxicant_conc' is not defined`。
*   **解决方案**：
    *   批量替换错误变量名为原始数据列名 `conc`。
    *   在脚本中添加异常捕获机制，提升代码健壮性。

---

### 问题 2：相关性分析图回归线与误差棒显示异常
*   **现象**：回归线无法拟合数据，误差棒长度与原图不符。
*   **解决方案**：
    *   确认 carbaryl 数据的单位（mg/L）与原图一致，避免单位换算错误。
    *   参考 `functions.R` 中的正交回归函数，重新拟合回归线。

---

### 复现结论
1.  两种检测平台对 carbaryl 的毒性评估结果相关性与论文完全一致，验证了方法的可靠性。
2.  核心代码逻辑清晰，添加注释后新手可快速上手，复现成本较低。
